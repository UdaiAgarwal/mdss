This MDSS is developed in ElectronJS. Check for the packages required for this version of mdss in package.json file
To install these packages run the following command after going into this folder

    npm install
